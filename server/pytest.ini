[pytest]
testpaths = tests
pythonpath = .
addopts = --cov=app --cov-fail-under=55 -m "not integration"
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    integration: marks tests as requiring Docker/testcontainers (deselect with '-m "not integration"')
