# PLACEHOLDER ONLY - Never commit real secrets to source control.
# Secrets are managed via Azure Key Vault and synced by the CSI Secret Store driver.
# This file is committed as documentation only; the actual K8s Secret is created
# by the SecretProviderClass defined in secret-provider-class.yaml.
#
# Required secrets (stored in Azure Key Vault, NOT here):
#   DATABASE_URL
#   FRONTEND_URL
#   JWT_SECRET_KEY
#   INVITE_TOKEN_SECRET
#   TOKEN_ENC_KEY_B64
#   LINKEDIN_CLIENT_ID
#   LINKEDIN_CLIENT_SECRET
#   LINKEDIN_REDIRECT_URI
#   GITHUB_APP_ID
#   GITHUB_ORG
#   GITHUB_CLIENT_ID
#   GITHUB_CLIENT_SECRET
#   GITHUB_REDIRECT_URI
#   SENDGRID_API_KEY
#   EMAIL_FROM
#   STRIPE_SECRET_KEY
#   STRIPE_SUCCESS_URL
#   STRIPE_CANCEL_URL
#   STRIPE_WEBHOOK_SECRET
#   MYSQL_ROOT_PASSWORD
#   MYSQL_DATABASE
#   MYSQL_USER
#   MYSQL_PASSWORD
apiVersion: v1
kind: Secret
metadata:
  name: ip-factory-secrets
  namespace: ip-factory
type: Opaque
# data: managed by CSI Secret Store driver via SecretProviderClass
