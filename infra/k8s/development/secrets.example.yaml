# Example secrets configuration for TheDataBay
# Copy this file to secrets.yaml and fill in actual values

apiVersion: v1
kind: Secret
metadata:
  name: thedatabay-secrets
type: Opaque
stringData:
  # PostgreSQL settings (shared by app + SonarQube in local dev)
  POSTGRES_URL: "postgresql+psycopg://thedatabay:thedatabay@postgres-svc:5432/thedatabay_db" # SQLAlchemy database URL for app server
  POSTGRES_DB: "thedatabay_db" # PostgreSQL initial database
  POSTGRES_USER: "thedatabay" # PostgreSQL username
  POSTGRES_PASSWORD: "thedatabay" # PostgreSQL password

  # App settings
  APP_NAME: "TheDataBay API" # Application name
  APP_VERSION: "0.1.0" # Application version
  ENVIRONMENT: "development" # Environment: development, staging, production

  # Server settings
  HOST: "0.0.0.0" # Server host (use 0.0.0.0 for k8s)
  PORT: "8080" # Server port

  # CORS settings
  CORS_ORIGINS: '["http://localhost:5173"]' # Allowed origins for CORS (JSON array as string)

  # LLM settings
  LLM_PROVIDER: "ollama" # LLM provider implementation
  LLM_BASE_URL: "http://host.docker.internal:11434" # Ollama-compatible base URL (chat + embeddings default)
  LLM_EMBEDDING_BASE_URL: "" # Override embedding endpoint; leave empty to share LLM_BASE_URL
  LLM_CHAT_MODEL: "deepseek-v4-flash:cloud" # Chat model name
  LLM_EMBEDDING_MODEL: "nomic-embed-text" # Embedding model name
  LLM_EMBEDDING_DIMENSION: "768" # Vector dimension produced by LLM_EMBEDDING_MODEL
  LLM_THINK: "false" # Enable model reasoning traces when supported
  OLLAMA_API_KEY: "" # Optional Ollama API key for hosted endpoints

  # Dataset summary settings
  DATASET_SUMMARY_COUNT: "5" # Number of dataset summaries to generate
  DATASET_SUMMARY_SAMPLE_ROWS: "20" # Number of sample rows per summary

  # Embedding job settings
  MAX_FILE_SIZE_MB: "50" # Maximum file size in megabytes
  MAX_DATASET_ROWS: "50000" # Maximum number of rows per dataset

  # Similarity search settings
  TOP_K: "10" # Number of top similar datasets to return
  SIMILARITY_THRESHOLD: "0.75" # Similarity score threshold
  CACHE_MAXSIZE: "100" # Maximum number of embeddings to cache

  # Pinata IPFS settings
  PINATA_API_KEY: "" # Your Pinata API Key
  PINATA_SECRET_KEY: "" # Your Pinata Secret Key
  PINATA_GATEWAY_URL: "https://gateway.pinata.cloud" # Pinata Gateway URL

  # Smart contract settings
  CONTRACT_ADDRESS: "0xYourContractAddressHere" # Your smart contract address
  USDC_TOKEN_ADDRESS: "0xYourUsdcTokenAddressHere" # ERC-20 payment token address
  CADC_TOKEN_ADDRESS: "0xYourCadcTokenAddressHere" # ERC-20 CADC payment token address
  CONTRACT_ABI_PATH: "/code/app/contracts/Marketplace.json" # Path inside server container
  CHAIN_ID: "31337" # Chain ID (31337 for local Anvil)
  # Tilt/Kubernetes backend pods cannot reach a host Anvil node through 127.0.0.1.
  # Keep `make anvil` running on the host, and use a host-reachable URL here.
  RPC_URL: "http://host.docker.internal:8545" # Your Ethereum RPC URL
  SERVER_PRIVATE_KEY: "" # Server signer private key (hex)
